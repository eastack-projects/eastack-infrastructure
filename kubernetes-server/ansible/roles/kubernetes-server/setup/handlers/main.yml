---
- name: Restart kubernetes server service
  systemd:
    name: "{{ item }}"
    state: restarted
    enabled: true
    daemon_reload: true
  with_items:
  - kube-controller-manager
  - kube-scheduler
  - kube-apiserver
